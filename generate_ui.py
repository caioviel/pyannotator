#!/usr/bin/env python
# -*- coding: utf-8 -*-

import shutil
import os

DIRECTORY = "ui"


if __name__ == "__main__":
    all_files = os.listdir(DIRECTORY)
    for mfile in all_files:
        if ".ui" in 
            print mfile